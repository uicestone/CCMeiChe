extends layout
block content
  h1.h1 #{subtitle}
  ul.choices
    for choice in choices
      li.row(data-id="#{choice._id}")
        .price #{choice.price}cc
        .promo #{choice.title}
  .button 提交
  +facade('recharge')